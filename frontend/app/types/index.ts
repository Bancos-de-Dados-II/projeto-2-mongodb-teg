export type CountryWithFlag = {
  name: string;
  unicodeFlag: string;
};

export type MyIcon = {
  url: string;
  size: [number, number];
};

export type Titulo = {
  nome: string;
  conquistas: number;
}
